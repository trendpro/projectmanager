<span class="contacts-tags-edit"> 
   <%= text_field_tag 'contact[tag_list]', "#{@contact.tags.map(&:name).join(',').html_safe}", :size => 10, :class => 'hol', :id => "allowSpacesTags" %><%= tagsedit_for('#allowSpacesTags', Contact.available_tags(:project => @project).map(&:name).join("\',\'").html_safe ) %>
</span> 
